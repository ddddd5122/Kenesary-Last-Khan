# Инициализация для главного меню
# Этот файл гарантирует, что игра всегда показывает главное меню при запуске

init python:
    # Отключаем автозагрузку последнего сохранения
    config.auto_load = None
    
label before_main_menu:
    # Эта метка вызывается перед показом главного меню
    return

label after_load:
    # Эта метка вызывается после загрузки сохранения
    # Мы не хотим автоматически загружать, так что просто возвращаемся в меню
    return

label quit:
    # Переопределяем выход, чтобы вернуться в главное меню
    return
